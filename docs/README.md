jpb-download

Jnj Python Backend for Download


```sh
cd C:\dev\shared\backends\jpb-download && xgit -e make -u jnjsoftone -n jpb-download -d "Jnj Python Backend for Dowonload file(image, audio, video / text, binary) to local directory"
```